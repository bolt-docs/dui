/**
 * Vitest setup for @dui-toolkit/plugin-diff.
 *
 * Color detection is handled via `FORCE_COLOR=1` set at the top
 * of `vitest.config.ts`, which makes `@bdocs/dui`'s
 * `isColorSupported` IIFE initialize to `true` at module-load.
 *
 * Reserved for future shared fixtures / lifecycle hooks.
 */
export {};
